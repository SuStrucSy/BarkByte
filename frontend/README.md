# Frontend

## Development

Install dependencies and start the Vite app:

```bash
npm install
npm run dev
```

## Environment

The frontend reads configuration from Vite environment variables:

- `VITE_API_URL`: Base URL for the backend API.

Example:

```bash
VITE_API_URL=http://localhost:8000
```
